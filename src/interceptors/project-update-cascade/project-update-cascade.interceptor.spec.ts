import { ProjectUpdateCascadeInterceptor } from './project-update-cascade.interceptor';

describe('ProjectUpdateCascadeInterceptor', () => {
  it('should be defined', () => {
    expect(new ProjectUpdateCascadeInterceptor()).toBeDefined();
  });
});
