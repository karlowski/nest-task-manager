import { TaskDeleteCascadeInterceptor } from './task-delete-cascade.interceptor';

describe('TaskDeleteCascadeInterceptor', () => {
  it('should be defined', () => {
    expect(new TaskDeleteCascadeInterceptor()).toBeDefined();
  });
});
