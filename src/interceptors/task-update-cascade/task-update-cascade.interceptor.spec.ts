import { TaskUpdateCascadeInterceptor } from './task-update-cascade.interceptor';

describe('TaskUpdateCascadeInterceptor', () => {
  it('should be defined', () => {
    expect(new TaskUpdateCascadeInterceptor()).toBeDefined();
  });
});
